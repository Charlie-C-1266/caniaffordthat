/** Total number of scrollytelling steps (0: mode select ... 4: result). */
export const STEP_COUNT = 5

/** Progress-rail labels, exact copy from the design prototype's `renderVals()`. */
export const STEP_LABELS = ['Start', 'Item', 'Budget', 'Timeframe', 'Result']
